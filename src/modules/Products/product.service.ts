import { Injectable } from '@nestjs/common';
//import { ProductRepository, TProduct } from './product.repository';

@Injectable()
export class ProductsService {
  /*   constructor(private productRepository: ProductRepository) {}
  getProducts(page: number, limit: number) {
    return this.productRepository.getProductsM(page, limit);
  }

  getProductById(id: number) {
    return this.productRepository.getProductByIdM(+id);
  }

  createProduct(product: TProduct) {
    return this.productRepository.createProductM(product);
  }

  updateProduct(id: number, product: TProduct) {
    return this.productRepository.updateProductM(id, product);
  }

  deleteProduct(id: number) {
    return this.productRepository.deleteProductM(id);
  } */
}
